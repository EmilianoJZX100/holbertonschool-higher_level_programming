#!/usr/bin/python3
"""divides all elements of a matrix"""


def matrix_reloaded(matrix, div):
    """function"""
    if div is not int or float:
        raise TypeError("div must be a number")
    if div = 0:
        raise ZeroDivisionError("division by zero")
